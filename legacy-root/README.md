This folder contains the older root-level GRPO stack that used to live at the repository top level.

It was moved here to keep the active project focused on `model-train4/`.

Current day-to-day work should happen in `model-train4/`.
