# =============================================================================
# requirements.txt — GSPO + Qwen3-Omni (Thinking) via ms-swift
# =============================================================================
# Установка:
#   pip install -r requirements.txt
#
# Flash-Attention ставится отдельно (требует CUDA toolkit):
#   pip install flash-attn --no-build-isolation
#
# Проверено на: Python 3.10/3.11, CUDA 12.1, A100/H100
# =============================================================================

# ---------------------------------------------------------------------------
# Core framework
# ---------------------------------------------------------------------------
ms-swift[llm]>=3.0.0

# ---------------------------------------------------------------------------
# PyTorch — Blackwell требует CUDA 12.8+
# RTX PRO 6000 Blackwell: используйте cu128
# ---------------------------------------------------------------------------
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0

# ---------------------------------------------------------------------------
# Transformers & friends
# ---------------------------------------------------------------------------
transformers>=4.51.0
tokenizers>=0.21.0
accelerate>=0.34.0
peft>=0.11.0
datasets>=2.20.0
trl>=0.9.0

# ---------------------------------------------------------------------------
# QLoRA — 4-bit квантизация NF4
# ---------------------------------------------------------------------------
bitsandbytes>=0.44.0

# ---------------------------------------------------------------------------
# DeepSpeed (multi-GPU / ZeRO)
# ---------------------------------------------------------------------------
deepspeed>=0.14.0

# ---------------------------------------------------------------------------
# Qwen3-Omni multimodal dependencies
# ---------------------------------------------------------------------------
qwen-vl-utils>=0.0.8          # image/video preprocessing для Qwen-VL семейства
soundfile>=0.12.1              # аудио I/O
librosa>=0.10.0                # аудио фичи
av>=12.0.0                     # PyAV — видео/аудио декодинг
Pillow>=10.0.0                 # обработка изображений
torchcodec>=0.1.0              # видео декодинг через PyTorch (опционально)

# ---------------------------------------------------------------------------
# Numerical & utilities
# ---------------------------------------------------------------------------
numpy>=1.26.0
scipy>=1.13.0
einops>=0.8.0
sentencepiece>=0.2.0
tiktoken>=0.7.0
protobuf>=3.20.0

# ---------------------------------------------------------------------------
# Logging & monitoring
# ---------------------------------------------------------------------------
tensorboard>=2.17.0
wandb>=0.17.0                  # опционально, замените tensorboard на wandb если нужно

# ---------------------------------------------------------------------------
# Serialization & data
# ---------------------------------------------------------------------------
safetensors>=0.4.3
pyarrow>=16.0.0
orjson>=3.10.0

# ---------------------------------------------------------------------------
# Dev / evaluation utilities
# ---------------------------------------------------------------------------
evaluate>=0.4.0

# ---------------------------------------------------------------------------
# Code execution sandbox (для code_reward в reward_functions.py)
# Раскомментируйте нужное:
# ---------------------------------------------------------------------------
# e2b>=0.17.0                  # облачный sandbox (рекомендуется для продакшена)
# RestrictedPython>=7.0        # локальный ограниченный exec

# ---------------------------------------------------------------------------
# Optional: vLLM-Omni-backed generation для ускорения GSPO rollout
# ---------------------------------------------------------------------------
vllm==0.16.0
vllm-omni==0.16.0
